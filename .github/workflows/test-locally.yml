name: Local Testing & Validation

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      
      - name: Set up Python 3.10
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install scikit-learn joblib pytest pytest-cov mlflow 2>&1 || pip install --upgrade --force-reinstall scikit-learn joblib pytest pytest-cov mlflow
      
      - name: Run unit tests
        run: |
          python -m pytest tests/ -v --cov=src --cov-report=xml
      
      - name: Upload coverage reports
        if: always()
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
        continue-on-error: true
      
      - name: Test training script locally
        run: |
          python src/train.py
      
      - name: Verify model artifact
        run: |
          if [ -f models/model.pkl ]; then
            echo "✅ Model artifact created successfully"
          else
            echo "❌ Model artifact not found"
            exit 1
          fi
